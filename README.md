# ML-CI-Project
A simple Machine Learning project demonstrating Continuous Integration (CI) using GitHub Actions. The workflow automatically installs dependencies, runs ML model tests using Pytest, and validates the project whenever code is pushed to the main branch.
